import java.util.Arrays;
public class Main {
 public static void main(String[] args) {
 int[] arr = {5, 2, 8, 1, 3};
 Arrays.sort(arr);
 System.out.println(Arrays.toString(arr));
 }
}



public class CheckSorted
{
public static void main (String[] args)
{
int[] array={1,2,3,4,5};
boolean isSorted=true;

for(int i=1; i<array.length; i++);
{
if (array[i] < array[i-1])
{
isSorted=false;
}
}

if(isSorted)
{
System.out.println("Array is sorted.");
}
else
{
System.out.println("Array is not sorted.");
}
}
}



Explanation:
 boolean isSorted = true: Initialize a flag `isSorted` as true to track if the array is
sorted.
 for (int i = 1; i < array.length; i++): Loop through the array, starting from the
second element.
 if (array[i] < array[i - 1]): If the current element is smaller than the previous one,
set `isSorted` to false and break the loop.
 Based on the value of `isSorted`, print whether the array is sorted or not.
